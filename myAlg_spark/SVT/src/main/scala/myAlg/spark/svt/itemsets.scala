package myAlg.spark.svt

class HSets(transactions: List[char]) {
  // val long list items
  val tid: Int = transactions(0)
  val itemSets: Array[Int] = transactions.tail.toArray
  // def -
}

class VSets() {
  //val long tid sets
  //val support
  //val item name
  //def +
  //def -
}
